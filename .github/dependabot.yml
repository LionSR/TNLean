version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      github-actions:
        patterns: ["*"]
    ignore:
      # This pin is owned by the gh-aw compiler. Update the compiler and
      # regenerate docs-blueprint-sync.lock.yml instead of editing it directly.
      - dependency-name: github/gh-aw-actions/setup
