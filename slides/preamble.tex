% ══════════════════════════════════════════════════════════════════════
% TNLean Presentation Preamble
% Consolidated from presentation20260212.tex (reference)
% ══════════════════════════════════════════════════════════════════════

% ── Packages ──
\usepackage{amsmath,amssymb,braket}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes.geometric,calc,decorations.pathreplacing,fit,backgrounds,matrix}
\usepackage{xcolor}
\usepackage{array}
\usepackage{listings}
\usepackage{appendixnumberbeamer}
\usepackage{booktabs}
\usepackage{colortbl}         % \rowcolor{} in tables
\usepackage[normalem]{ulem}  % \sout{} for strikethrough

% ── Bibliography (natbib) ──
\usepackage[numbers,sort&compress]{natbib}
\bibliographystyle{unsrtnat}

% ── Color scheme (dark theme) ──
\definecolor{leangreen}{RGB}{34,180,34}
\definecolor{leanblue}{RGB}{70,130,180}
\definecolor{amber}{RGB}{255,191,0}
\definecolor{softred}{RGB}{220,80,80}
\definecolor{darkbg}{RGB}{21,21,21}
\definecolor{cardbg}{RGB}{30,30,30}
\definecolor{dimwhite}{RGB}{180,180,180}
\definecolor{deepblue}{RGB}{50,100,170}
\definecolor{mintgreen}{RGB}{100,210,130}

% ── Beamer colors ──
\setbeamercolor{background canvas}{bg=darkbg}
\setbeamercolor{normal text}{fg=white}
\setbeamercolor{title}{fg=white}
\setbeamercolor{frametitle}{fg=white,bg=darkbg}
\setbeamercolor{block title}{fg=white,bg=gray!30!black}
\setbeamercolor{block body}{fg=white,bg=cardbg}
\setbeamercolor{alerted text}{fg=amber}
\setbeamercolor{example text}{fg=leangreen}
\setbeamercolor{itemize item}{fg=leangreen}
\setbeamercolor{itemize subitem}{fg=dimwhite}
\setbeamercolor{itemize subsubitem}{fg=dimwhite}
\setbeamercolor{enumerate item}{fg=leangreen}
\setbeamercolor{progress bar}{fg=leangreen}
\setbeamercolor{title separator}{fg=leangreen}
\setbeamercolor{footline}{fg=dimwhite,bg=darkbg}

% ── Footer with page number / total ──
\setbeamertemplate{footline}{%
  \hfill%
  \usebeamercolor[fg]{footline}%
  \usebeamerfont{footline}%
  \insertframenumber\,/\,\inserttotalframenumber%
  \hspace*{6pt}\par%
}

% ── Lean listing style ──
\lstdefinelanguage{Lean}{
  keywords={theorem, lemma, def, abbrev, variable, namespace, end, open, import,
            noncomputable, private, section, structure, where, let, have, exact,
            intro, apply, simp, rw, rcases, refine, classical, cases, with,
            fun, forall, exists, Prop, Type, sorry, by, instance, require},
  keywordstyle=\color{leanblue}\bfseries,
  keywords=[2]{IsInjective, SameMPV, SameMPV2, GaugeEquiv, GaugePhaseEquiv,
               MPSTensor, Matrix, Fin, GL, CanonicalForm, AlgEquiv,
               TwoSidedIdeal, transferMap, mixedTransferMap,
               IsIrreducibleCP, IsCP, IsPrimitiveCP, IsOrthogonalProjection,
               HasUniqueFixedPoint, spectralRadius, mixedTransferSpectralRadius,
               PosSemidef, PosDef, toTensorFromBlocks, evalWord, mpv,
               Submodule, Set, AlgHom, LinearMap,
               HopfieldNet, CPTPMap, IsCompletelyPositive,
               choi_PSD_iff_CP_map, pft_irreducible, spectral_dominance_of_primitive,
               IsIrreducible, IsPrimitive,
               piAlgEquiv, perBlockLinearExtension, mpvFun},
  keywordstyle=[2]\color{leangreen},
  comment=[l]{--},
  commentstyle=\color{gray}\itshape,
  string=[b]",
  stringstyle=\color{orange},
  basicstyle=\ttfamily\scriptsize\color{white},
  backgroundcolor=\color{cardbg},
  breaklines=true,
  frame=single,
  rulecolor=\color{gray!50!black},
  showstringspaces=false,
  escapeinside={(*@}{@*)},
  literate=
    {ℕ}{{$\mathbb{N}$}}1
    {ℂ}{{$\mathbb{C}$}}1
    {ℝ}{{$\mathbb{R}$}}1
    {→}{{$\to$}}1
    {←}{{$\leftarrow$}}1
    {⟨}{{$\langle$}}1
    {⟩}{{$\rangle$}}1
    {∀}{{$\forall$}}1
    {∃}{{$\exists$}}1
    {⊤}{{$\top$}}1
    {⁻¹}{{${}^{-1}$}}2
    {≠}{{$\neq$}}1
    {≤}{{$\leq$}}1
    {≥}{{$\geq$}}1
    {∈}{{$\in$}}1
    {∘}{{$\circ$}}1
    {σ}{{$\sigma$}}1,
}
\lstset{language=Lean}

% ── Common macros ──
\newcommand{\proved}[1]{\textcolor{leangreen}{\checkmark\ #1}}
\newcommand{\inprog}[1]{\textcolor{amber}{$\circ$\ #1}}
\newcommand{\future}[1]{\textcolor{gray}{$\diamond$\ #1}}
\newcommand{\notproved}[1]{\textcolor{softred}{$\boldsymbol{\times}$\ #1}}
\newcommand{\sorrymark}[1]{\textcolor{softred}{\texttt{sorry}\ #1}}
\newcommand{\notavail}[1]{\textcolor{softred}{$\boldsymbol{\times}$\ #1}}
\newcommand{\avail}[1]{\textcolor{leangreen}{$\checkmark$\ #1}}
\newcommand{\MD}{M_D(\mathbb{C})}
\newcommand{\tr}{\mathrm{tr}}
\newcommand{\spn}{\mathrm{span}}
\newcommand{\GL}{\mathrm{GL}}
\newcommand{\GLC}{\mathrm{GL}}
\newcommand{\CP}{\mathrm{CP}}
\newcommand{\rank}{\mathrm{rank}}
\newcommand{\diag}{\mathrm{diag}}
\newcommand{\community}[1]{\textcolor{deepblue}{$\blacklozenge$\ #1}}