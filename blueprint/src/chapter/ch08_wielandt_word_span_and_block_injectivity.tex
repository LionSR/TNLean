\section{One-step padding and exact word spans}\label{sec:cumspan_to_wordspan}

Cumulative spanning, $T_N(A)=\MN{D}$, is weaker than exact-length spanning
$S_L(A)=\MN{D}$. If $\Id\in S_1(A)$, shorter words can be padded to a
common length. Section~\ref{sec:app_wielandt_identity_in_span} proves this
conversion and records a counterexample without the one-step padding
condition.

\begin{theorem}[Homogeneous word-span propagation under normalization]%
    \label{thm:wordspan_propagation_unital}
    \lean{MPSTensor.wordSpan_succ_eq_top_of_unital_of_wordSpan_eq_top}
    \lean{MPSTensor.wordSpan_eq_top_of_ge_of_unital}
    \lean{MPSTensor.isNBlkInjective_of_ge_of_unital}
    \leanok
    \uses{def:word_span, def:l_blk_injective}
    Suppose
    \begin{align}
        \sum_i A^i(A^i)^\dagger
        &=\Id,
        \label{eq:wielandt_unital_normalization}
    \end{align}
    and $S_L(A)=\MN{D}$.
    Then $S_m(A)=\MN{D}$ for every $m\ge L$.
    Equivalently, $L$-block injectivity propagates to every larger
    homogeneous length.
\end{theorem}

\begin{proof}\leanok\uses{lem:word_span_mul}
    If $S_n(A)=\MN{D}$, then for every $X\in\MN{D}$,
    substituting (\ref{eq:wielandt_unital_normalization}) gives
    \begin{align}
        X
        &=\left(\sum_i A^i(A^i)^\dagger\right)X
          =\sum_i A^i((A^i)^\dagger X)
          \in S_1(A)S_n(A)
          \subseteq S_{n+1}(A).
        \notag
    \end{align}
    Indeed, $(A^i)^\dagger X\in S_n(A)$ because
    $S_n(A)=\MN{D}$.
    Thus $S_{n+1}(A)=\MN{D}$.
    Iterating this implication gives the result for every $m\ge L$.
\end{proof}

\begin{theorem}[Algebra span plus one-step padding implies normality]%
    \label{thm:algspan_to_normal}
    \lean{MPSTensor.isNormal_of_algSpan_eq_top_of_aperiodic}
    \leanok
    \uses{def:normal, def:alg_span, def:word_span}
    If $\algspn(A)=\MN{D}$ and $\Id\in S_1(A)$, then $A$ is normal.
\end{theorem}

\begin{proof}\leanok\uses{thm:cumspan_to_normal}
    Since $\algspn(A)=\MN{D}$, the ascending chain condition for
    finite-dimensional subspaces gives an $N$ such that
    $T_N(A)=\MN{D}$.
    Apply Theorem~\ref{thm:cumspan_to_normal}.
\end{proof}

\begin{remark}[Normal canonical form avoids one-step padding]\label{rem:d1_bypasses_aperiodicity}
    The identity-in-the-one-step-span results in
    Section~\ref{sec:app_wielandt_identity_in_span} are needed only when
    cumulative spanning is upgraded to exact-length spanning.
    The normal-canonical theory used later in
    Chapters~\ref{ch:canonical}--\ref{ch:bnt} does not pass through this
    step: it works directly with irreducible TP-normalized blocks whose
    blocked transfer maps are primitive and, once the corresponding weighted
    block data are available, places them in normal canonical form in the
    sense of~\cite{Cirac2016MPDO_arXiv}.
    There are analogous cumulative-span lemmas for blocked irreducible tensors
    and eigenvector-spreading arguments, but they are not needed for the
    implication stated below.
\end{remark}

\section{Block injectivity from TP/primitive/irreducible blocks}%
\label{sec:ch07_block_inj_from_tp_primitive_irred}

\begin{theorem}[TP-primitive irreducible tensor is injective after blocking]%
    \label{thm:tp_primitive_irred_block_injective}
    \lean{MPSTensor.exists_pos_blockTensor_isInjective_of_tp_primitive_irreducible}
    \leanok
    \uses{thm:normal_tp_primitive_irred,
        lem:one_block_injective_of_injective,
        cor:blk_inj_zero_dim_one}
    Let $A$ be a left-canonical MPS tensor with $D>0$.
    Suppose that $A$ is tensor-irreducible and that its transfer map is
    primitive.
    Then there exists a positive blocking length~$L$ such that
    $A^{[L]}$ is one-site injective.
\end{theorem}

\begin{proof}\leanok
    \uses{thm:normal_tp_primitive_irred,
        lem:one_block_injective_of_injective,
        cor:blk_inj_zero_dim_one}
    If $D=1$, trace preservation gives a nonzero one-site Kraus matrix, and
    every nonzero scalar matrix spans $\MN{1}$.
    Hence $A$ is one-site injective, so
    Lemma~\ref{lem:one_block_injective_of_injective} gives the positive
    witness $L=1$.
    Suppose now that $D\ge2$.
    Theorem~\ref{thm:normal_tp_primitive_irred} gives a
    block-injectivity witness~$L$.
    If $L=0$, Corollary~\ref{cor:blk_inj_zero_dim_one} would force $D=1$,
    a contradiction.
    Thus $L>0$, and the blocked-chain equivalence identifies this
    $L$-block injectivity with one-site injectivity of the blocked tensor
    $A^{[L]}$.
\end{proof}

\begin{remark}
    Blocking preserves normality by Theorem~\ref{thm:isNormal_blockTensor},
    proved in Section~\ref{sec:app_wielandt_blocking_support}.
\end{remark}
