\chapter{Canonical Form Reduction: Supporting Results}
\label{ch:canonical_supporting_results}

This appendix supports Chapter~\ref{ch:canonical}. It collects the projection, gauge, blocking, compression, and reindexing arguments used by the main reduction while keeping the source-named canonical-form statements and the principal reductions in the chapter.

Throughout, $\E_A(X)=\sum_iA^iX(A^i)^\dagger$, $A^{[p]}$ denotes blocking by $p$, and $V^{(N)}(A)_\sigma=\tr(A^{\sigma_1}\cdots A^{\sigma_N})$. Weighted direct sums are written $\bigoplus_k\mu_kA_k$.

\input{appendix/ft_mps/ch09_canonical_splitting_and_gauges}
\input{appendix/ft_mps/ch09_canonical_blocking_and_sectors}
