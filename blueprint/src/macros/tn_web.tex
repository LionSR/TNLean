% Tensor-network web fallbacks are rendered by
% Packages/tn_diagrams.py and plastex_templates/TensorNetworkDiagrams.jinja2s.
%
% The chapter-facing \TN... vocabulary is the same in print and web output.
% Its complete figures live in macros/tn_print.tex, while its semantic core
% and reusable atoms live in ../../tex/tn/.  The web renderer compiles these
% sources to cached SVG files.
