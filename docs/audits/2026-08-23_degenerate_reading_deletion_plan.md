# Ranked deletion plan: degenerate-reading apparatus in TNLean (origin/main db49b8610)

> **Status.** This document preserves a planning audit; it is not an adopted
> deletion program. Items marked “Owner decision” and all items in Section D
> remain unresolved. No deletion is authorized by this document alone.

Verification pass (rg, read-only, worktree `cache-qiclean`): all ten load-bearing claims below checked. Corrections to the surveys: `**Scope restriction (...)**` stamps = **594 lines in 191 files** (not 610/195); `ActiveSector` with word boundary = 108 occurrences / 8 files (the 352/21 figure counted substrings such as `IsActiveSector`, `CyclicActiveSector`, `ActiveSectorSpanning*`); `PhysicalSectorFactorization` is referenced in **101** files (not ~60). Everything else held: `IsNonvanishingSourceSimple` lives in exactly 4 Lean files (docstring line 107: "not part of CPSV16 Definition 4.7"); `IsSimple` has zero hypothesis consumers (only def, two bridges, `M_isSimple`, `R_not_isSimple`); `SimpleVanishingCounterexample` and `RescalingStableNotSimple` are imported only by `TNLean/MPS/MPDO.lean`; `exists_isBNTCanonicalForm_afterBlocking_pos` has 0 Lean consumers (1 blueprint `\lean`, 2 glossary lines); weak `ProportionalMPV₂` has 1 consumer (`PeripheralProportionalCaseRootFromRescaling`), which itself has 0 consumers; no `_status` label is ever `\ref`'d (0 hits, 14 labels); `completedBntSectorLabel` is literally `if hk : hη.p k ≠ 0 then … else s₀`; `cyclicActiveWeight` is literally `if IsCyclicActiveSector k then 1 else 0`; QICLean `EntropyMarkovForward.lean:18,237` pads with zero-weight sectors whose "density fillers" are "invisible"; source `propSN` (l.1406-1411) gives an *isometry* U and Prop 3to5 (l.1529) divides by `a_k b_h`; l.219 prints `∑D_k ≤ D (i.e., there can be zero blocks)`.

---

## (A) DELETE-AS-DEGENERATE waves (ranked: certainty first, then size)

### A1. MPDO simplicity: unit-weight witness baked into Definition 4.7, plus invented nonvanishing strengthening
- **Degenerate reading retired.** (i) Reading CPSV16 Def 4.7 "simple" as requiring a line-246 unit-weight BNT witness of the blocked tensor, which the source's own Def-4.1-normalized Example 4.12 (CF weights 1/√2, 1/√2) and the dimer R (weight √(337/512)) fail; (ii) the invented clause "closed MPO ≠ 0 at every positive length", separated from `IsSourceSimple` only by the diag(1,-1) tensor whose "density operator" is zero at odd N.
- **Delete.** `MPOTensor.IsSimple` (`TNLean/MPS/MPDO/SimpleTensor.lean:223`) + bridges `IsSimple.isSourceSimple`, `IsSimple.isNonvanishingSourceSimple` (`SourceSimpleTensor.lean:161,184`); `IsNonvanishingSourceSimple` + `.isSourceSimple`, `.mpo_ne_zero`, `.smul_ofReal`, `isNonvanishingSourceSimple_smul_ofReal_iff` (`SourceSimpleTensor.lean:110`, `SourceSimpleScaling.lean:165-197`); `R_isNonvanishingSourceSimple` (`RescalingStableSourceSimple.lean:117`); whole modules `SimpleVanishingCounterexample.lean` (267) and `RescalingStableNotSimple.lean` (155, incl. `R_not_isSimple`, `IsHorizontalCF.eq_one_of_transferMap_comp_self_eq_smul`, deprecated alias); `IsBNTCanonicalForm.eq_one_of_transferMap_comp_self_eq_smul` (`RFP/BNTOrthogonality.lean:447`, sole consumer is the deleted module). Blueprint: `def:mpdo_simple_tensor`, `thm:mpdo_simple_implies_source_simple`, `def:mpdo_nonvanishing_source_simple_tensor`, `thm:mpdo_simple_implies_nonvanishing_source_simple_of_nonzero`, `thm:mpdo_nonvanishing_source_simple_positive_rescaling(_iff)`, `thm:mpdo_simple_vanishing_source_simple_status`, `thm:bnt_unit_weight_quasi_idempotent_scalar`, `thm:mpdo_horizontal_cf_quasi_idempotent_scalar` (ch21_mpdo_rfp_simple_definition.tex); `R_not_isSimple` tags at ch21_mpdo_rfp_bnt_coefficients.tex:796,1105. Docs: glossary, README, `cpsv16_simple_tensor_nilpotency.tex`, the predicate-comparison paragraphs of `cpsv16_unit_weight_rfp_scale_tension.tex` (see B9).
- **Convention replacing it.** `IsSourceSimple` (scale-invariant nilpotency on a BNT sector presentation; already `\leanok` as `def:mpdo_source_simple_tensor`) becomes the only predicate and is **renamed `IsSimple`** (mathematical rename, no `@[deprecated]` alias per CONTRIBUTING). Positive-length nontriviality stays a theorem (`IsSourceSimple.exists_mpo_ne_zero`). Markers in `RescalingStableLengthDependentRFP*.lean:205,321`, `RescalingStableSourceSimple.lean:87`, `MPU/Simple.lean:98` lose their "does not assert IsSimple R" sentences.
- **Net:** about −600 Lean, −10 blueprint nodes. **Risk:** low (0 hypothesis consumers; 6 files touch `IsSourceSimple`). **Owner decision:** confirm the rename `IsSourceSimple → IsSimple` (one line: yes/no).

### A2. Conditional "user supplies the line-246 normalization" supplier and the zero-family scope marker
- **Degenerate reading retired.** Refusing to commit to l.246 ("we can always choose |μ_k| ≤ 1 and at least one equals one, something which we will assume from now on") and handing both clauses back to the caller; and treating the zero positive-length MPV family (the zero tensor) as a flagged scope restriction rather than the convention.
- **Delete.** `MPSTensor.exists_isBNTCanonicalForm_afterBlocking_pos` + docstring (`SectorBNT/Supplier.lean:740-800`); blueprint `thm:paperbnt_supplier_after_blocking` (appendix ft_mps/ch10_bnt_block_separation_and_suppliers.tex:300-303), `rem:arbitrary_input_reduction_gap` (ch10_bnt_sector_canonical_form.tex:1197) and its `\ref`s at :1200,1229; glossary.md:486-489; the `**Scope restriction (nonzero MPV family)**` stamp on `exists_isBNTCanonicalForm_afterBlocking_pos_normalized` (`SupplierNormalized.lean:184-188`), the matching blueprint paragraph (ch10_bnt_sector_canonical_form.tex:1232-1237), and the "separate zero-family clause" elimination plan in `docs/paper-gaps/cpsv16_cf_normalization_and_proportional_comparison.tex` (~l.320-337; the rest of that note stays).
- **Convention replacing it.** `exists_isBNTCanonicalForm_afterBlocking_pos_normalized` with its single hypothesis `∃ N > 0, ∃ σ, mpv A σ ≠ 0` **kept as a plain hypothesis, unstamped**: the nonzero family is the standing convention, not a sub-case. (Surveys 2 and 3 disagreed on the marker; the owner rule settles it: a hypothesis excluding the canonical degenerate input is the convention, not a scope restriction.)
- **Net:** about −120 lines. **Risk:** none (0 consumers). **Owner decision:** none.

### A3. Weak proportionality `c_N = 0` predicate and its only consumer
- **Degenerate reading retired.** "Proportional MPV families" with `c_N` allowed to vanish (`V = 0·V` is no projective point; `cpsv16_nonzero_proportionality_reading.tex`, resolved).
- **Delete.** `MPSTensor.ProportionalMPV₂` (`MPS/Defs.lean:124`), bridge `NonzeroProportionalMPV₂.toProportionalMPV₂` (:165), blueprint `def:proportional_mpv` (ch02_mps.tex:329) and its `\uses` entry at :372; the two docstring mentions in `FundamentalTheorem/Proportional.lean:18,251` reworded; the stale "remains available for older auxiliary lemmas" sentence in the note. Together with it: `PeripheralProportionalCaseRootFromRescaling` + `peripheralProportionalCase_periodicFT_of_rootFromRescaling` (`Periodic/FundamentalTheorem.lean:321-342`, 0 consumers, a "assume a rescaling principle" Prop), blueprint `thm:peripheral_periodic_ft_proportional_rescaling` (ch22_periodic_ft_overlap_sector_match_and_consequences.tex:392), docstring :380-381, and the `dccsp17_periodic_overlap_route_alignment.tex:46` sentence.
- **Convention replacing it.** `NonzeroProportionalMPV₂` (Defs.lean:139) is already the definition-level convention; the live proportional periodic route goes through `Periodic/ProportionalOverlap.lean` and `peripheralProportionalCase_periodicFT_of_sameMPV₂Pos`.
- **Net:** about −70 lines. **Risk:** none. **Owner decision:** delete the zero-consumer conditional periodic node, or retarget its premise to `NonzeroProportionalMPV₂` and keep (one line).

### A4. Counterexample modules and printed-status nodes whose witness is outside the source's standing convention
- **Degenerate reading retired.** Refuting printed claims with witnesses the authors' conventions exclude: non-canonical-form representations with hidden nilpotent bond sectors, zero tensors, identical duplicate blocks, zero-multiplicity product sectors, d = 1 readings the next sentence of the source rules out.
- **Delete.**
  - `thm:cpsv_theorem44_printed_status` (ch21_mpdo_rfp_foundations.tex:972) + `thm:global_prfp_not_source_zcl` / `thm:nonzero_global_prfp_not_source_zcl` (:992,1012) + `exists_isPRFP_not_isSourceZCL`, `exists_isPRFP_isMPDO_physTraceTransfer_ne_zero_not_isSourceZCL` (`LocalPurificationRFP.lean:587,729`, ~220 lines): witnesses are the zero tensor and Q = [[1,0,0],[0,0,1],[0,0,0]] whose MPO is the scalar 1 at every length; excluded by l.217-246 ("we can write it in CF in terms of some BNT"). Reclassify `cpsv16_purification_rfp_definition.tex` from false-source to open-gap (faithful Def 4.1 PRFP predicate still missing); update `docs/counterexamples.md:178`, `docs/audits/2026-05-08-mps-ft-paper-coverage.md:133`.
  - `TNLean/MPS/MPU/SourceVCounterexample.lean` (263; docstring concedes "no positive-definite transfer fixed point … no canonical-form-II presentation with full active support"), 14 refs in ch28_mpu.tex, `docs/counterexamples.md:190`.
  - `TNLean/MPS/MPDO/BiCFDerivation/Counterexample.lean` (134; two identical 1×1 blocks violate the BNT pairwise-non-equivalence convention; an independence-of-fields justification, not a refutation), 2 refs in ch20_mpdo_canonical_forms_intro_finite_separation.tex, `counterexamples.md:166`.
  - `TNLean/MPS/RFP/PhaseMultiplicityCounterexample.lean` (148; d = 1 virtual-only reading of display III_CFI_RFP, excluded by l.559-563) + `thm:phase_flip_tensor_not_rfp`; **relocate** `scalarUnitTensor_isNormalTensor` to `SectorBNT/Examples.lean` (3 consumers: `RFP/PhaseOscillation.lean:167`, `RFP/GaugeBlockingCounterexample.lean:225`, `RFP/BNTWeightCounterexample.lean:159`); strip the "naive fixed-i virtual-block interpretation" sentence from `thm:cpsv_charact_mps_status`.
  - `thm:cpsv_theorem414_printed_status` (ch21_mpdo_rfp_fusion_isometries_product_laws.tex:348, `\notready`): its complaint is "not well-defined on empty product sectors"; `BNTFusionTensorClause` already has `multiplicity_pos`/`weight_pos` (`BNTFusionTensorClause.lean:84,88`), so `isRFPViaTS_iff_hasBNTFusionTensorClause` *is* Theorem 4.14 under the convention. Drop the node, the `(iii_act)` subscript and the "Scope restriction (active product BNT)" stamp on `thm:mpdo_cpsv_bnt_rfp_equivalence`; coisometry orientation is already a resolved local-correction note.
- **Convention replacing it.** None needed beyond the standing CF/BNT conventions; the surviving `\leanok` theorems stand alone.
- **Net:** about −800 Lean, −4 blueprint nodes, ~30 blueprint refs. **Risk:** low-medium (ch28_mpu.tex prose rewrite; purification note reclassification). **Owner decision:** confirm that "refuted only on a representation the source's CF convention excludes (hidden nilpotent sector)" counts as degenerate, which decides the Theorem 4.4 and MPU/SourceV items together (one line).

### A5. Hayashi/MPDO physical sectors: zero-probability padded sectors (cross-repo, staged)
- **Degenerate reading retired.** Treating the complement of the HJPW Markov support as sectors of probability zero. Source: Lemma Lsigma3 (l.1351-1363) decomposes the support with q_j > 0; propSN (l.1406-1411) outputs an *isometry* U and primitive T; SALZCL (l.1484-1499) makes T = a bᵀ rank one, so every a_k b_h > 0 and Prop 3to5 (l.1529) divides by a_k b_h without comment. The padding is manufactured in QICLean (`EntropyMarkovForward.lean:18,237`: zero-weight sectors with "invisible density fillers", `m := (dB − n) + K`) so that `decompB` is an equivalence on all of `Fin dB` and U_B is unitary; TNLean copies the shape (`PhysicalSectorFactorization.lean:67-71`: `sectorEquiv : Fin d ≃ Σ k …`, square `physicalIsometry`).
- **Convention replacing it.** `p k > 0` as a field; rectangular coisometry `U` with `U Uᴴ = 1` plus the reconstruction identity `K = Uᴴ(⊕ l_k ⊗ r_k)U`, exactly the shape the vertical CF already adopted after #3869. Then `IsActiveSector ≡ True`, `ActiveSector p = Fin sectorCount`, `zeroWeightReparameterized… = id`, `activeSectorTraceMatrix = sectorTraceMatrix`, `completedBntSectorLabel = bntSectorLabel`, inactive/completed densities vanish.
- **Sub-waves (in dependency order):**
  - **A5a (QICLean, first).** `HayashiMarkovDecomposition` (`hp_nonneg` → `hp_pos`, unitary U_B → coisometry, exhaustive `decompB` → support-indexed) and `hayashi_ssa_equality_characterization_forward`. ~400 lines. Cross-repo.
  - **A5b.** `MPOTensor.PhysicalSectorFactorization` fields (:53-83); `EtaStructure` (`SimpleLocalStructure.lean:497`, 20 files). 101 files reference `PhysicalSectorFactorization`; most only pass it through.
  - **A5c.** Completed BNT labels: `completedBntSectorLabel`, `completedBntSectorProjection(_isOrthogonal/_mul_eq_zero)`, `sum_completedBntSectorProjection` (`BNTSeparatingProjectors.lean:45-120,252`, 4 stamps), `activeMarkovProjection` (`BNTMarkovSectorProjectors.lean:459-476`, 3 stamps); blueprint ch21_mpdo_rfp_simple_local_markov_bnt_compression.tex:288,641-668; note section l.288-324 of `cpgsv17_mpdo_sal_zcl_eta_local_structure.tex`. ~150 lines.
  - **A5d.** Inactive-pair completions of the 1/(a_k b_h) quotient: `inactiveThreeSiteNeighboringDensity`, `completedThreeSiteNeighboringDensity`, `completedThreeSitePreparationMap`, `completedThreeSiteControlledMap` (`PhysicalSectorOmegaPreparation.lean:160-354`, 6 stamps), `inactiveEtaDensity`/`inactiveOmegaDensity` (`EtaPreparation.lean:276-300`); add `0 < a k * b h` to `NeighboringTraceFactorization` (`NeighboringPreparation.lean:48-59`); delete note `cpgsv17_mpdo_zero_weight_preparation_completion.tex`, blueprint labels `thm:mpdo_physical_sector_three_site_neighboring_operator_properties`, `def:mpdo_physical_sector_t1_preparation`, `thm:mpdo_physical_sector_t1_density_properties`, `thm:mpdo_physical_sector_t1_preparation_cptp`, and `…normalized_preparations_completed_channels.tex:283,339`. ~300 lines.
  - **A5e.** Zero-weight pruning/reparameterization: `PhysicalSectorPruning.lean` (203), `zeroWeightReparameterizedInverseMapPhysicalSectorFactorization` family (6 files / 34 occ; `InverseMapActiveSectorRecurrence.lean:82-257`, `InverseMapActiveSectorPrimitivity.lean:38-107`); blueprint ch21_…neighboring_bond_contractions.tex:419-512 (4 tags), …coherent_positive_rephasing.tex:569-571. ~500 lines.
  - **A5f.** Active subtype and restricted trace matrix: `ActiveSector`, `ActiveSectorEntryIndex`, `activeSectorOneSiteMatrixFamily`, `activeSectorTraceMatrix` (`PhysicalSectorTraceMatrix.lean:34-36,245`; 163 occ / 11 files), `ActiveSectorTraceMatrixZCL.lean` (125), `InverseMapActiveSectorPrimitivity.lean:109-295`, `InverseMapActiveSectorZCL.lean` (181), ActiveSector-typed restatements in `LemmaC5CaseI.lean`/`InverseMapLemmaC5CaseI.lean`; blueprint ch21_mpdo_rfp_simple_local_primitivity_active_trace_matrix.tex (1532 lines, ~20 tags, keep only the A-S-Spanning section l.473-565 renamed). ~1,100 lines.
  - **A5g.** Inactive branch of the active-support compression (`IsActiveSector`, "set both coordinate dimensions to zero", empty fibers) in `PhysicalSectorActiveFactorSupport.lean:62` and capstone.tex:550-689. ~400 lines; the rest of that chain is D2.
- **Net:** about −2,500 to −3,900 Lean, most of two blueprint chapters (1532 + 521 lines), 2 notes, ~20 `Local fix (inactive sectors)/(zero-weight quotient)` stamps. **Risk:** HIGH (cross-repo; 101-file structure change; proofs in A5c-A5f must be restated, not just deleted). **Owner decision:** approve the QICLean convention change (support-indexed sectors, `p > 0`, coisometric U) before any TNLean work (one line). If approved, D1 (cyclic-active layer) likely collapses too.

---

## (B) CONSOLIDATE-MARKERS waves (math stays; stamps become one per family per module)

| # | Family | Stamps (verified) | Where the single marker lives | Net |
|---|---|---|---|---|
| B1 | `cyclic-active restriction` 131 + `cyclic-active visibility` 4 + `selected-sector visibility` 10 | 145 across 12 CyclicActive*/Selected* files, all citing `cpsv16_commuting_form_to_sal.tex` | module docstring of `CyclicActiveSectorRestriction.lean` + one cross-reference line in the other module headers | −400 |
| B2 | `BNT-refined horizontal form` | 31 in 25 MPDO files, all citing `cpgsv17_vertical_cf_grouping.tex` (resolved) | docstring of `IsHorizontalCF` (`HorizontalBNT.lean:139`); keep the six `_of_cpsvCanonicalForm` wrappers | −90 |
| B3 | `retained vertical coordinates` 19 + `positive chains of length at least two` 15 | 30 in `TopologicalGibbsHamiltonian.lean`, 4 in TopologicalPhysicalGibbs/CPSVTopologicalPhysicalGibbs | two sentences in the module docstring | −90 |
| B4 | `zero-sector complement` (vertical) | 4 in `VerticalSectorTraceLoss.lean:427-560`, 7 in `BNTAlgebraTensorClauseConditionalPhysicalMaps.lean:517-703` | one module note pointing at `cpgsv17_vertical_isometry_zero_sector.tex`; keep coisometry, reconstruction identity, `supportCompletion`, image-preservation lemmas | −35 |
| B5 | `periodic-boundary comparison` 11 + `doubly normalized specialization` 9 + `crossing span` 6 | 26 across 7 ParentHamiltonian BNTBlockDiagonal* files | hub docstring of `BNTBlockDiagonalPGVWCComparison.lean` | −75 |
| B6 | `closing matrices` | 11 (9 in `BNTMarkovSectorProjectors.lean`, 1 each PhysicalSupportRestriction/BNTProjectorSelection) | bundle "every closing matrix ≠ 0" as a field of the closing-matrix data (or derive once from simplicity+SAL+common weight) and drop the hypothesis from the 9 lemmas | −35 |
| B7 | `supplied unitary sector conjugacies` 8, `conditional reflected target` 8, `uniform physical and bond dimensions` 8, `active physical support compression` 8, `full-support fusion family` 8, `length-(L_0)` 7, `invertible gauge` 6, `T-cover` 5, `per-block separation` 5, `fixed representative` 5, `chain length` 5 | each confined to 1-4 modules, one note each | one docstring sentence per module; `fixed representative` stamps (`SimpleTensor.lean:182-189,219-222` on `IsSimpleCanonicalForm`, which stays: 4 files) shrink to one sentence once A1 lands | −180 |
| B8 | Blueprint status-node duplicates (none `\ref`'d) | `thm:cpsv_rfp_gauge_printed_status` (dup of `\leanok` `thm:cpsv_pure_rfp_gauge_status`), `thm:cpsv_theorem_zcl_pure_status` + `thm:cpsv_main_mps_status` (same two witnesses), `thm:cpsv_theorem49_printed_status`, `thm:cpsv_prop45_printed_status`, `thm:cpsv_examples410_412_status` (→ remarks), `thm:cpsv_prop_c14_printed_status` (→ Scope-restriction marker on `thm:mpdo_canonical_bnt_proportional_sectors_zcl_sal`), `rem:lemma2b_status` + `rem:blocked_lemma2b_gap` (merge), `thm:cpsv_fibonacci_open_boundary_rank_status` (rename label; it is a proved theorem), `thm:cpsv_charact_mps_status` (keep as open-gap record after the A4 edit) | −400 blueprint |
| B9 | Notes and comment residue | `cpsv16_unit_weight_rfp_scale_tension.tex`: keep §1-3, §5-7, drop predicate-comparison/§8 "three distinct predicates" after A1 (−200); `cpsv16_global_vs_persector_unit_witness.tex` (258) → one sentence in the `weight_unit_exists` docstring + README line; 11 `% Provenance: source-label audit completed in issue #5917/#5920` lines and 2 `% Per-sector unit-modulus restriction eliminated` comments (ch11_fundamental_theorem_core.tex:59-61,379-381) | −470 |
| B10 | Rule | Add to CLAUDE.md §Locally-fixable deviations: "one marker per (family, module), on the module docstring or defining declaration; per-declaration stamps only when the restriction differs from the module's" | +5 |

B total: about −1,000 Lean marker lines, −870 blueprint/note lines. Risk: low (no statements change except B6). Owner decision needed only for B6 (field vs derive-once) and B10 (adopt the rule).

---

## (C) GENUINE-KEEP (surveys were tempted; do not re-propose)

1. `IsBNTCanonicalForm.weight_norm_le_one` / `weight_unit_exists` (`SectorBNT/Basic.lean:145,160`): the convention-as-field form; `weight_norm_le_one` load-bearing at source l.1244. Optional hygiene: `basisCount_pos` lemma so 5 proofs stop destructuring the unit witness for nonemptiness.
2. `CPSVCanonicalFormData.IsWeightNormalized` separate from `CPSVCanonicalFormData` (`CanonicalForm/Definitions.lean:283`): the MPDO scale is pinned by Def 4.1 and the dimer R / Example 4.12 CFs are literal CFs without a unit witness. Real scale subtlety.
3. `IsCPSVBasisOfNormalTensors` (37 files) vs `IsBNTSectorPresentation` (2 files): only two one-line bridges duplicated; owner already ruled the literal interface stays. At most fold `IsBNT` (`MPS/BNT/Basic.lean:80`, 3 call sites) into the literal predicate.
4. `SameMPV` / `SameMPV₂` / `SameMPV₂Pos` / `SameMPVFrom`: distinct relations (mpv([]) = D is source-faithful, per project decision); the reverse bridge was already removed.
5. `cpsv16_proportional_length_scalar_gap.tex` + `coeff_identity_via_matched_mpv_phase_proportional`, `halvedDecomp`: C ⊕ ½C vs C ⊕ ⅓C, nonzero weights, refutes an over-strong library claim.
6. `cpsv16_zero_tail_length_zero_decomposition.tex`: already a completed instance of the rule (#2153/#2156/#2167); 0 `zeroTail*` declarations remain.
7. Downstream `hc : c ≠ 0` in Reduction/BNTRefinement.smul_left/TPGauge/etc.: genuine rescaling lemmas, not repeated convention.
8. `cpsv16_example_4_12_normalization.tex` + `CPSVExample412Literal/NormalizedRFP.lean`: T_M² = 2T_M, a printed wrong constant on nondegenerate data.
9. `IsSourceZCL` (λ > 0, 34 files) vs `IsPhysicalTraceIdempotent` (λ = 1, 4 files): normalization axis, the ≠ 0 clause is the definition-level convention. (Minor dedup of `_of_literal_ZCL` is D5.)
10. `cpsv16_ft_one_copy_scope_restriction.tex`: honest scope restriction, resolved.
11. Counterexamples on nondegenerate data: `Theorem49RepeatedCopyCounterexample` (weights 1, 1/2), `CaseIIAbsorptionCounterexample` (1/√2, 1), `GaugeBlockingCounterexample` (cube-root phase), `BNTWeightCounterexample` (1, 1/2 and Bell pair), `ThermodynamicLimitCounterexample` (parity MPDO), Examples 4.10-4.12, Fibonacci rank chain, `AlgebraFusionCounterexample`, `PositiveMinimalRealization` + `PositiveLinearExtension`, `BiCFDerivation/DiagonalRestrictionCounterexample`. Only their duplicate status nodes go (B8).
12. Vertical zero-sector complement math (`VerticalSectorTraceLoss`, `supportCompletion`, coisometry + reconstruction): source prints `∑D_k ≤ D` (l.219) and "isometry U" (l.946); witness |0…0⟩⟨0…0| is an honest MPDO; Def 4.1 needs channels on the full algebra. Markers only (B4).
13. The 13 remaining `\gapnote{false-source}` notes (all but `cpsv16_purification_rfp_definition.tex`, reclassified in A4).
14. `IsSimpleCanonicalForm` (`SimpleTensor.lean`): live hypothesis of the Case II route (`BNTSectorAnalyticProperties.lean:168`, `CaseIIAbsorptionCounterexample.lean:561,936`); source's "K in CF and simple" at l.1628-1665.

---

## (D) UNSURE: deciding question per item

- **D1. Cyclic-active layer** (16 `CyclicActive*.lean`, 7,241 lines; `IsCyclicActiveSector`, `cyclicActiveWeight` 0/1 indicator fed into `ActiveSector p`; blueprint ch21_mpdo_rfp_commuting_form_cyclic_active_markov.tex 1,355 lines). Under propSN/SALZCL every η_{k,h} ≠ 0, so every sector is cyclic-active and the layer is vacuous. **Decides:** is the converse Prop 4to2 (l.1597-1600) formalized on arbitrary commuting-form factorizations (then keep as a proof device, rebuilt without `ActiveSector p` after A5, consolidated per B1) or only on the propSN/SALZCL output class (then delete with A5)?
- **D2. Within-sector column-support compression** (`PhysicalSectorActiveRestriction.lean` 998, `PhysicalSectorActiveNeighboring.lean` 418, `PhysicalSectorActiveBond.lean` 132; note `cpsv16_active_physical_support_compression.tex`, open). The inactive branch is A5g. **Decides:** for support-indexed Hayashi sectors, are the joint column supports of {l_{k,β}}, {r_{k,α}} automatically the full factor spaces? If yes the 1,933-line chain is an identity and goes; if no it stays as an honest library auxiliary with its note.
- **D3. `ActiveSectorSpanningCounterexample.lean`** (584) + `ActiveSectorInverseMapProvenance.lean` (483) + AreaLaw/RFP companions (~2,094 lines; ~44 blueprint refs). Four-sector positive-weight tensor with T primitive, T² ≠ T, refuting the `cpgsv17_pf_rank_one.tex` step; but the same tensor has a one-sector factorization. **Decides:** does the provenance file's claim stand that the four sectors are what the source's own Hayashi + inverse-map procedure selects (genuine) or is the four-sector presentation nonminimal (degenerate, delete)?
- **D4. `NonCartesianActiveSector*` cluster** (4 files) and `VerticalCoefficientPresentationCounterexample.lean` (705). Both refute project-invented low-level lemmas with the source's standing normalization / CF hypothesis dropped; no Lean consumer beyond `MPDO.lean`. **Decides:** is the (ii)⇒(iv) route through the low-level lemma, resp. the "relabelled coefficient equality" conjecture, still pursued? If not, delete (~840 lines, 15 blueprint refs).
- **D5. `_of_literal_ZCL` duplicates** (9 theorems, 6 files) and `IsPhysicalTraceIdempotent`. **Decides:** can every λ = 1 consumer normalize first via `IsSourceZCL`? If yes collapse into the source-ZCL versions (~150 lines). Low priority.
- **D6. `full-support fusion family`** (8 stamps; `TopologicalProjectors.lean`, `BNTFusionIsometries.lean`): field `U†U = 1` "excludes a common zero corner". **Decides:** can `TopologicalProjectors.lean` consumers take the coisometric `BNTFusionIsometries` directly? If yes delete the full-support structure in favour of the coisometric one (vertical convention after #3869).
- **D7. Duplicate line-246 normalization lemmas** (`SupplierNormalized.lean:132` vs `NormalReduction/WeightNormalization.lean:45`, 1 consumer each). **Decides:** is the positive-real clause of the PGVWC07 version needed at `WeightNormalization.lean:194`? Minor dedup, not a degenerate-reading item.
- **D8. `PEPS/*Counterexample*.lean` (3), `Archive/*Counterexample*.lean` (3).** Not audited; Archive is excluded from root imports; PEPS ones are backed by resolved false-source notes. **Decides:** a header read of the three PEPS modules against `peps_gauge_edge_scalars.tex` / `peps_gaugeConsistency_connectivity_gap.tex`.

---

## Suggested execution order
A2 → A3 → A1 → A4 → B8/B9 (blueprint/notes, same PR stream as A1/A4) → B2/B3/B5/B7 (pure docstring PRs, mechanical) → B1 (after D1 decided) → A5a (QICLean) → A5b-g → D2/D3 (after A5 settles what the sectors are) → B6/B10.

Totals if every A wave lands: about −4,100 to −5,500 Lean/blueprint lines; B: about −1,900 lines; D1-D4 potentially another −10,000 pending the four decisions.
