% Negative: the retired wire `via=` waypoint key has no parser path; a
% waypoint is an invisible junction atom on a chained wire.
\documentclass{standalone}
\usepackage{tenkz}
\begin{document}
\tenkzkernel
\begin{tenkz}[rows={wire}, cols=3, bonds=none]
  \tn[at=(1,1), skin=box, name=a]{A}
  \tn[at=(1,3), skin=box, name=b]{B}
  \tnwire[kind=string, via={(1,2)}]{a}{b}
\end{tenkz}
\end{document}
